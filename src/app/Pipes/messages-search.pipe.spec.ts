import { MessagesSearchPipe } from './messages-search.pipe';

describe('MessagesSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new MessagesSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
