import { CarsSearchPipe } from './cars-search.pipe';

describe('CarsSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new CarsSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
